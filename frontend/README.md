# Vue 3 Admin Template

一个基于 Vue 3 + Vite + Vuetify 3 的现代化管理后台模板，简洁、高效、易于定制。已精简优化，移除所有第三方推广内容，专注于提供干净专业的后台管理基础框架。

## ✨ 特性

- 🚀 **Vue 3.5** - 最新的 Vue 3 框架，使用 Composition API
- ⚡️ **Vite 5** - 极速的开发体验和构建性能
- 🎨 **Vuetify 3.7** - Material Design 组件库
- 🌍 **国际化** - 内置中英文支持，默认中文（vue-i18n）
- 🎯 **TypeScript Ready** - 完整的类型支持
- 📦 **自动导入** - 组件和 API 自动导入
- 🗂 **文件路由** - 基于文件系统的路由
- 🎭 **主题切换** - 支持深色/浅色主题，优化的深色主题配色
- 📊 **图表集成** - ApexCharts 数据可视化
- 🔐 **认证页面** - 精简优化的登录页面
- ⚡️ **性能优化** - 首次加载性能优化，实时性能监控

## 📦 技术栈

### 核心框架
- **Vue 3.5.13** - 渐进式 JavaScript 框架
- **Vite 5.4.11** - 下一代前端构建工具
- **Vuetify 3.7.5** - Material Design 组件库

### 状态管理 & 路由
- **Vue Router 4.5** - 官方路由管理器
- **Pinia 2.3** - 轻量级状态管理

### 国际化 & 工具
- **Vue I18n 9** - 国际化解决方案
- **VueUse** - Vue Composition 工具集
- **Iconify** - 统一的图标框架
- **ApexCharts** - 现代化图表库
- **Web Vitals** - 性能监控

### 开发工具
- **ESLint** - 代码质量检查
- **Prettier** - 代码格式化
- **Stylelint** - 样式代码检查
- **Sass** - CSS 预处理器

## 📁 项目结构

```
├── src/
│   ├── @core/              # 核心组件和样式
│   │   ├── components/     # 核心组件（卡片、按钮等）
│   │   ├── scss/          # 核心样式文件
│   │   └── utils/         # 核心工具函数
│   ├── @layouts/          # 布局系统
│   │   ├── components/    # 布局组件
│   │   └── styles/        # 布局样式
│   ├── assets/            # 静态资源
│   │   ├── images/        # 图片资源
│   │   └── styles/        # 全局样式
│   ├── components/        # 公共组件
│   ├── layouts/           # 页面布局
│   │   ├── default.vue    # 默认布局（带侧边栏）
│   │   └── blank.vue      # 空白布局（登录页等）
│   ├── locales/           # 国际化语言包
│   │   ├── zh-CN.js       # 中文语言包
│   │   └── en.js          # 英文语言包
│   ├── pages/             # 页面文件（自动路由）
│   │   ├── dashboard.vue  # 仪表盘
│   │   ├── login.vue      # 登录页
│   │   ├── account-settings.vue
│   │   ├── card-basic.vue
│   │   ├── tables.vue
│   │   ├── form-layouts.vue
│   │   ├── typography.vue
│   │   ├── icons.vue
│   │   └── [...error].vue # 404 错误页
│   ├── plugins/           # 插件配置
│   │   ├── vuetify/       # Vuetify 配置
│   │   │   ├── theme.js   # 主题配置
│   │   │   ├── defaults.js
│   │   │   └── icons.js
│   │   ├── i18n.js        # 国际化配置
│   │   ├── router/        # 路由配置
│   │   └── pinia.js       # 状态管理
│   ├── utils/             # 工具函数
│   │   ├── resourceLoader.js      # 资源加载器
│   │   └── performanceMonitor.js  # 性能监控器
│   ├── views/             # 视图组件
│   │   ├── dashboard/     # 仪表盘组件
│   │   ├── pages/         # 页面组件
│   │   └── user-interface/ # UI 组件
│   ├── App.vue            # 根组件
│   └── main.js            # 入口文件
├── public/                # 公共静态资源
├── .vscode/              # VS Code 配置
├── package.json          # 项目配置
├── vite.config.js        # Vite 配置
└── README.md             # 项目说明
```

## 🚀 快速开始

### 环境要求

- **Node.js** >= 16.0.0
- **Yarn** 或 **npm** 或 **pnpm**

### 安装依赖

```bash
# 使用 Yarn（推荐）
yarn install

# 或使用 npm
npm install

# 或使用 pnpm
pnpm install
```

### 开发模式

```bash
# 启动开发服务器
yarn dev

# 或
npm run dev
```

访问 http://localhost:5173

### 生产构建

```bash
# 构建生产版本
yarn build

# 或
npm run build
```

### 预览构建结果

```bash
# 预览生产构建
yarn preview

# 或
npm run preview
```

访问 http://localhost:5050

### 代码检查

```bash
# ESLint 检查并自动修复
yarn lint

# 或
npm run lint
```

## 📄 页面说明

| 页面 | 路径 | 说明 |
|------|------|------|
| 仪表盘 | `/` 或 `/dashboard` | 数据可视化仪表盘，包含图表和统计卡片 |
| 登录 | `/login` | 用户登录页面（已优化精简） |
| 账户设置 | `/account-settings` | 用户账户设置页面 |
| 卡片 | `/card-basic` | 各种卡片组件展示 |
| 表格 | `/tables` | 数据表格组件展示 |
| 表单布局 | `/form-layouts` | 各种表单布局示例 |
| 排版 | `/typography` | 文字排版展示 |
| 图标 | `/icons` | 图标库展示 |
| 错误页 | `/404` | 404 错误页面 |

## 🎨 主题配置

### 主题文件位置
`src/plugins/vuetify/theme.js`

### 自定义颜色

```javascript
export const themes = {
  light: {
    colors: {
      'primary': '#8C57FF',
      'secondary': '#8A8D93',
      'success': '#56CA00',
      'info': '#16B1FF',
      'warning': '#FFB400',
      'error': '#FF4C51',
      'background': '#f4f5fa',
      'surface': '#fff',
      // ... 更多颜色
    }
  },
  dark: {
    colors: {
      'primary': '#8C57FF',
      'background': '#1a1d29',  // 优化的深色背景
      'surface': '#232734',
      // ... 更多颜色
    }
  }
}
```

### 深色主题特点
- 背景色：`#1a1d29`（深蓝灰色）
- 表面色：`#232734`
- 优化的对比度和可读性
- 现代化的配色方案

## 🌍 国际化

### 默认语言
项目默认语言为**中文（zh-CN）**，支持中英文切换。

### 语言包位置
- 中文：`src/locales/zh-CN.js`
- 英文：`src/locales/en.js`

### 使用方法

在组件中使用：
```vue
<template>
  <div>{{ $t('common.login') }}</div>
</template>

<script setup>
import { useI18n } from 'vue-i18n'

const { t } = useI18n()
console.log(t('common.email'))
</script>
```

### 添加新语言

1. 在 `src/locales/` 创建新语言文件，如 `ja.js`
2. 在 `src/plugins/i18n.js` 中导入并注册：

```javascript
import ja from '@/locales/ja'

const messages = {
  'zh-CN': zhCN,
  'en': en,
  'ja': ja,  // 添加日语
}
```

## ⚡️ 性能优化

本项目已进行全面的性能优化，确保快速的首次加载和流畅的用户体验。

### 已实现的优化

#### 1. Vite 构建配置优化
- ✅ 依赖预构建优化（vue, vue-router, pinia, @vueuse/core, @iconify/vue, apexcharts 等）
- ✅ 启用缓存机制（避免重复构建）
- ✅ 代码分割策略（vue-vendor, vuetify-vendor, charts-vendor, utils）
- ✅ 文件名哈希（缓存控制）
- ✅ 构建优化（target: es2015, minify: esbuild, cssCodeSplit: true）
- ✅ 开发服务器优化（warmup, hmr）

#### 2. 资源加载优化
- ✅ 资源加载器模块（`src/utils/resourceLoader.js`）
- ✅ 预连接外部域名（preconnect）
- ✅ 预加载关键资源（preload）
- ✅ 延迟加载非关键资源（字体、图标）
- ✅ 使用 requestIdleCallback 优化加载时机

#### 3. 性能监控
- ✅ 集成 Web Vitals 监控（FCP, LCP, FID, CLS, TTFB）
- ✅ 自定义指标监控（Vue 初始化时间）
- ✅ Performance Observer（资源加载、长任务检测）
- ✅ 性能报告生成
- ✅ 阈值警告

### 性能指标

**开发环境：**
- 开发服务器启动时间：869ms
- 依赖预构建使用缓存

**监控目标：**
- FCP (First Contentful Paint): < 1.8s
- LCP (Largest Contentful Paint): < 2.5s
- FID (First Input Delay): < 100ms
- CLS (Cumulative Layout Shift): < 0.1
- TTI (Time to Interactive): < 3.8s
- TTFB (Time to First Byte): < 600ms

### 查看性能报告

在浏览器控制台执行：
```javascript
performanceMonitor.generateReport()
```

### 性能优化文件

- `src/utils/resourceLoader.js` - 资源加载器
- `src/utils/performanceMonitor.js` - 性能监控器
- `vite.config.js` - Vite 优化配置
- `src/main.js` - 优化的入口文件

## 🎯 核心功能

### 1. 自动导入
- **组件自动导入**：无需手动 import 组件
- **API 自动导入**：Vue、Vue Router、Pinia API 自动可用
- **图标自动导入**：Iconify 图标按需加载

### 2. 文件路由
基于 `src/pages/` 目录的文件自动生成路由：
- `pages/dashboard.vue` → `/dashboard`
- `pages/login.vue` → `/login`
- `pages/[...error].vue` → 404 页面

### 3. 布局系统
- **default.vue**：带侧边栏的默认布局
- **blank.vue**：空白布局（用于登录、注册等页面）

### 4. 响应式设计
- 移动端优先
- 自适应各种屏幕尺寸
- 优化的触摸交互

## 🛠️ 开发规范

### 代码风格
- 使用 **ESLint** 进行代码检查
- 使用 **Prettier** 进行代码格式化
- 使用 **Stylelint** 进行样式检查

### 命名规范
- **组件命名**：PascalCase（如 `UserProfile.vue`）
- **文件命名**：kebab-case（如 `user-profile.vue`）
- **变量命名**：camelCase（如 `userName`）
- **常量命名**：UPPER_SNAKE_CASE（如 `API_URL`）
- **事件命名**：camelCase（如 `@filterChange`）

### Vue 3 最佳实践
- 优先使用 Composition API
- 使用 `<script setup>` 语法
- 合理使用 `ref` 和 `reactive`
- 避免过度使用 `watch`

## 🖥️ 浏览器支持

| Browser | Version |
|---------|---------|
| Chrome | 最新版本 ✅ |
| Firefox | 最新版本 ✅ |
| Safari | 最新版本 ✅ |
| Edge | 最新版本 ✅ |

支持所有实现最新 CSS 标准的现代浏览器。

## 📝 更新日志

### v2.3.0（当前版本）
- ✅ 移除所有第三方推广内容
- ✅ 添加完整的国际化支持（中英文）
- ✅ 优化深色主题配色
- ✅ 精简登录/注册页面
- ✅ 优化导航菜单
- ✅ 简化 Footer
- ✅ 全面性能优化
  - Vite 构建配置优化
  - 资源加载优化
  - 性能监控集成
  - 代码分割优化
- ✅ 修复语法错误
- ✅ 精简项目代码
- ✅ 合并优化文档

## 🔧 故障排除

### 开发服务器启动慢
- 清除 `node_modules/.vite` 缓存
- 重新安装依赖：`rm -rf node_modules && yarn install`

### 构建失败
- 检查 Node.js 版本（需要 >= 16.0.0）
- 清除缓存：`yarn cache clean`
- 重新安装依赖

### ESLint 错误
- 运行 `yarn lint` 自动修复
- 检查 `.eslintrc.cjs` 配置

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 License

MIT License

---

**注意**：本项目基于开源模板精简定制，已移除所有第三方推广内容，专注于提供一个干净、专业、易于定制的管理后台基础框架。
